import React from "react";
import FileUploader from "../FileUpload";

export default (
    <FileUploader
        className="Upload"
        labelText="Add files"
        buttonLabel="Upload Files"
        buttonKind="primary"
        multiple
        tabIndex={0}
        uxpId="FileUploader0"
    />
);